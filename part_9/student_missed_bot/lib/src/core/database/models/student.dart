/// Модель студента
base class StudentModel {
  final int id;
  final String fullName;
  final int groupId;

  StudentModel({
    required this.id,
    required this.fullName,
    required this.groupId,
  });
}
